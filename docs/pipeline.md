# pipeline — 진행·태스크 통합 관리

> 이 **한 파일**에서 페이즈·세부 태스크·진행상태를 모두 관리한다.
> 세션 시작·골든 루프·브랜치/커밋/테스트 규칙은 [AGENT_GUIDE.md](AGENT_GUIDE.md).
> 각 페이즈의 상세 설계(목표·변경 범위·완료 조건)는 [phases/](phases/) 파일.
>
> 체크 방법: 태스크 `- [ ] T#`를 구현→'✔ 검증' 초록 확인→`- [x]`.
> **페이즈 완료 = 그 페이즈 태스크 전부 [x]** → 현황표 상태·태스크 수 갱신.
> 상태: `▶ current` `✅ done` `⛔ blocked` `⬜ todo`

목표: Gemini vs OpenAI 임베딩을 정답 라벨로 채점(ARI·묶음 정확도)해 **최적 구성
선정** → 증분 중복 묶기·수집 파이프라인 검증 → plick-ai 이식.

## 현황표
| # | 페이즈 | 티켓 | 상태 | 태스크 |
|---|---|---|---|---|
| 01 | 프로젝트 뼈대 | — (이식 전) | ✅ done | — |
| 02 | PoC 이관 + 기준선 확립 | — (이식 전) | ✅ done | — |
| 03 | 정답 라벨 + 정량 평가 러너 | — (이식 전) | ✅ done | — |
| 04 | LLM Wiki(Obsidian) 구축 | — (이식 전) | ✅ done | — |
| 05 | 모델별 실험 (2인 병렬: Gemini/OpenAI) | B: KAN-182~186 · A: KAN-272~274 | ✅ done | B: 4/4 · A: 3/3 |
| 06 | 결과 종합 · 최적 구성 선정 | KAN-278·279 (KAN-3 하위) | ✅ done | 2/2 |
| 07 | 증분 중복 묶기 + 예외 케이스 | KAN-280·285·287·288 (T01~T04, KAN-3 하위) | ▶ current | 4/5 |
| 08 | 수집→임베딩→벡터 저장 파이프라인 | 미발행 | ⬜ todo | — |

> **컨벤션 이식(2026-07-15)**: agentic-starter 골든 루프 → plick-ai 방식(develop
> 브랜치 모델 + KAN 티켓 + pipeline.md)으로 전환. Phase 01~04는 이식 전에 완료돼
> 페이즈별 KAN 티켓이 없다. Phase 05 트랙 B(OpenAI)부터 에픽 **KAN-3 "[기능]
> AI 가공"** 하위 작업(Task) 티켓으로 관리한다. 상세는 [DECISIONS.md](DECISIONS.md).

## 완료된 페이즈 (01~04) — 상세는 phases/, 결과는 wiki/

- **01 프로젝트 뼈대**: uv·pyproject, `src/plick_embedding/{providers,pipeline,eval,report}`,
  settings(.env 로드), 러너 뼈대, ruff.
- **02 PoC 이관 + 기준선**: articles90(90건) 고정, Gemini 임베딩·병합형 군집화·
  최근 24시간만 비교·리포트. 기준선 SEMANTIC@0.85.
- **03 정답 라벨 + 채점기**: `data/labels/articles90.json`(56이슈), `eval/scoring.py`
  (ARI·묶음 정확도·잘못 합침/잘못 나뉨). 기준선 ARI 0.9071·F1 0.9091.
- **04 LLM Wiki**: `wiki/`(INDEX·모델·개념·실험 노트), 실행 시 노트 자동 생성
  (`report/wiki.py`), 과거 실험 4건 백필, 링크·목차 점검 테스트.

## 05 — 모델별 실험 (2인 병렬)
**목표**: 트랙 A(Gemini)·트랙 B(OpenAI)가 각자 실험 매트릭스를 독립 설계·실행하고
정답 라벨로 채점, 결과를 wiki에 같은 지표로 남겨 Phase 06 비교 재료를 만든다.
**상세**: [phases/05-parallel-experiments.md](phases/05-parallel-experiments.md) ·
**계획 노트**: `wiki/plans/`

**트랙 A — Gemini (담당: 본인이 인수)** · 티켓: 에픽 KAN-3 하위 작업
- [x] **T09** (KAN-272) Gemini 임베딩 실험·채점 — ✔ 8세트 실험 노트(wiki/experiments 2026-07-22)
  + 최고 구성 확인 (SEMANTIC·768·짧은요약 @0.86 ARI 0.9287)
- [x] **T10** (KAN-273) Gemini 구조 개선 실험 — ✔ 카테고리·주체 파티션 적용, 전후 비교
  (거의 효과 없음: 0.9287→0.9303, OpenAI +0.062와 대비). 신호 파일·파티션 군집화+테스트
- [x] **T11** (KAN-274) Gemini 최적 구성 선정 — ✔ `wiki/트랙A_Gemini_실험종합.md` +
  모델 노트 갱신. 최고 SEMANTIC·768·짧은요약 @0.86 ARI 0.9287

**트랙 B — OpenAI (담당: 본인)** · 티켓: 에픽 KAN-3 하위 작업(백로그)
- [x] **T05** (KAN-182) OpenAI 임베딩 실험 계획 — ✔ `wiki/plans/트랙B_OpenAI_매트릭스.md`
- [x] **T06** (KAN-183) OpenAI 임베딩 연동 — ✔ `providers/openai.py` + 러너 + 캐시 테스트(총 26개 초록)
- [x] **T07** (KAN-184) OpenAI 임베딩 실험·채점(짧은요약 4세트) — ✔ wiki 실험 노트 4개 + 최고 구성 확인
  (최고: large/768 @0.55 ARI 0.7545 · `scripts/sweep_threshold.py`)
- [x] **T08** (KAN-185) OpenAI 최적 구성 선정 — ✔ `wiki/models/text-embedding-3.md`에
  추천 구성(large·상세요약, 1536=최고점/768=안정)·기준값 유지 폭·비용·공정 비교 정리
- [x] **(KAN-186) 상세요약 임베딩 비교** — ✔ articles_detail90 스냅샷 + title_detail 4세트
  (최고 large/1536 @0.65 상세요약 ARI 0.8077, OpenAI 통틀어 최고). 비교는 text-embedding-3 노트

> 백로그 티켓(2026-07-15): KAN-182~186을 에픽 KAN-3 하위 작업으로 생성.
> T05·T06은 이미 구현 완료(코드/노트 존재) — 티켓 상태는 백로그(해야 할 일)로 두고
> 실제 착수 시 브랜치·상태 전환. 착수 전엔 생성만.

---

## 07 — 증분 중복 묶기 + 예외 케이스
**목표**: 새 기사가 하나씩 들어오는 운영 상황을 흉내 내 발행 시각 순으로 묶고,
지난 실험에서 드러난 예외(잘못 합침·여러 날 이어지는 이슈가 쪼개짐)를 실험으로
대응한다. **상세**: [phases/07-online-dedup.md](phases/07-online-dedup.md)

- [x] **T01** (KAN-280) 새 기사 하나씩 묶기 — ✔ `pipeline/incremental.py`
  (`cluster_incrementally`, 대표값 3안 centroid/latest/seed) + `scripts/run_incremental.py`
  + 결정적 테스트 10개. 선정 구성으로 90건 처리 확인(centroid ARI 0.8415, 배치 0.9287).
- [x] **T02** (KAN-285) 중복 묶기 처리 방식 비교 — ✔ `scripts/compare_grouping.py` +
  위키 노트 `배치_vs_순차_묶기_비교.md`. 위키 노트 이름 충돌 수정(방식 태그 online-<대표값>).
  **핵심: 순차가 사가를 더 쪼개는 게 아니라 더 합쳐 잘못 합침 1→4, ARI 0.9287→0.8415.**
  원인=롤링 평균 표류 → T03(대표값) 개선 후보.
- [x] **T03** (KAN-287) 묶음 대표값 3안 비교 — ✔ `scripts/sweep_representative.py` +
  위키 `순차_묶기_대표값_비교.md`. **첫 기사(seed) ARI 0.8636 > 평균 0.8415 > 최신 0.7031.**
  seed가 잘못 합침 최소(대표값 안 넓어짐) → 순차 기본값을 seed로(run_incremental 기본값·
  DECISIONS 기록). 대가로 잘못 나뉨 5→7(사가) → T05 몫. 배치(0.9287)와 격차는 T04·T05로.
- [x] **T04** (KAN-288) 애매한 구간 LLM 판정 — ✔ `judge.py`(OpenAIJudge)·`pipeline/gray_zone.py`·
  `scripts/run_grayzone.py` + 위키 `애매한_구간_LLM_판정.md`. 애매 구간(0.80~0.90)만 OpenAI에
  물음. **묶음 전체를 보여주는 게 핵심** — 첫 기사만 비교하면 사가 단계를 다른 사건으로 쪼개
  ARI 0.6925 붕괴, 묶음 전체+스토리 프롬프트로 고치니 **잘못 합침 3→0·ARI 0.8636→0.9149**
  (배치 0.9287 근접, 순차 최고). 남은 잘못 나뉨 7=사가 → T05. 판정 캐시(재실행 0원).
- [ ] **T05** 길게 이어지는 이슈 대응 (티켓 미발행)

---

## 로그 (최신 위)
- **P07-T04 완료(2026-07-23, KAN-288)**: 애매한 구간 LLM 판정 `judge.py`(OpenAIJudge,
  gpt-4o-mini)·`pipeline/gray_zone.py`·`scripts/run_grayzone.py` + 위키 `애매한_구간_LLM_판정.md`.
  애매 구간(0.80~0.90)에 걸린 결정만 OpenAI에 "같은 이슈?"를 물음(응답 캐시, 재실행 0원).
  **첫 시도(첫 기사만 비교)는 사가를 쪼개 ARI 0.6925로 붕괴** → 원인=LLM이 이적 단계(협상 vs
  메디컬)를 다른 사건으로 봄. **고침=묶음 전체를 보여주고 "이슈=이어지는 한 사건" 프롬프트**
  → 잘못 합침 3→0(대표값으로도 못 풀던 산투스+추가미드필더까지)·잘못 나뉨 7 유지·**ARI
  0.8636→0.9149**(배치 0.9287 근접, 순차 최고). 남은 나뉨 7=24h 범위 사가→T05. 판정 LLM은
  OpenAI(임베딩만 Gemini). 테스트 스텁으로 API 없이 8개. 브랜치 `feat/KAN-288-grayzone-llm`.
- **P07-T03 완료(2026-07-23, KAN-287)**: 묶음 대표값 3안 채점 `scripts/sweep_representative.py`
  + 위키 `순차_묶기_대표값_비교.md`. 선정 구성 90건: **첫 기사(seed) ARI 0.8636(잘못 합침 3·
  나뉨 7) > 평균 0.8415(4·5) > 최신 0.7031(3·7).** T02의 표류 가설 확정 — 평균은 큰 묶음에서
  대표값이 넓어져 잘못 합침↑, 최신은 대표값이 튀어 불안정. seed는 대표값 고정이라 잘못 합침
  최소·ARI 최고, 대가로 잘못 나뉨↑(T05). **순차 기본값을 seed로**(run_incremental 기본값 변경,
  DECISIONS 기록). 세 방식 공통 미해결 #6(산투스+추가 미드필더)=회색지대 LLM 몫(T04).
  브랜치 `feat/KAN-287-representative-compare`.
- **P07-T02 완료(2026-07-23, KAN-285)**: 배치 vs 순차 비교 `scripts/compare_grouping.py`
  + 위키 `배치_vs_순차_묶기_비교.md`. 선정 구성 90건: 배치 ARI 0.9287(묶음 62·잘못 합침 1·
  나뉨 6) vs 순차 0.8415(57·4·5), 두 방식 간 ARI 0.8777. **예상과 반대로 순차가 사가를 더
  쪼개지 않고 오히려 더 합침** — 롤링 평균(centroid)이 큰 이적 묶음에서 표류해 주변·후속
  기사를 끌어들임(산투스+추가 미드필더, 틸레망스+두 미드필더, 이라올라 2건, 홀란 3건).
  → T03에서 대표값 seed/latest로 표류 줄여보기. 부수: 위키 노트 이름에 방식 태그(online-<대표값>)
  추가해 배치·순차 노트가 안 덮어쓰게 수정(테스트 포함). 브랜치 `feat/KAN-285-batch-vs-online`.
- **P07-T01 완료(2026-07-22, KAN-280)**: 새 기사 하나씩 묶기 = `cluster_incrementally`
  (발행 시각 순으로 한 건씩 → 최근 24시간 안 살아 있는 묶음의 대표값과 비교 → 기준값
  넘으면 붙이고 갱신 시각 새로 찍기, 아니면 새 묶음). 대표값 3안(centroid/latest/seed)
  파라미터로 구현해 T03 재작업 없게. 라벨은 입력 순서에 맞춰 반환 → 기존 채점·리포트
  그대로 재사용. `scripts/run_incremental.py` + 결정적 테스트 10개(전체 43개 초록).
  선정 구성으로 90건 처리 확인: centroid ARI 0.8415 · F1 0.8454(잘못 합침 4·잘못 나뉨 5),
  배치(0.9287)보다 낮음 — 정식 비교는 T02. 위키 노트 이름 충돌 발견(T02에서 수정).
- **P06-T02·T04 완료 · Phase 06 종료(2026-07-22, KAN-279)**: 임베딩 최종 선정 =
  **Gemini SEMANTIC·768·짧은요약·기준값 0.86 (ARI 0.9287)**. 4축(정확도·안정성·비용·운영)
  근거·탈락 사유를 `wiki/최종선정_임베딩구성.md` + DECISIONS, 팀 공유 요약 CONFLUENCE_UPDATE
  §11. 마스터 설계가 이미 gemini-embedding-001 가정이라 무충돌. **Phase 06 done →
  Phase 07(증분 중복 묶기) ▶ current.** (팀 합의·재현 확인은 개발자 테스트로 남김.)
- **P06-T01 완료(2026-07-22, KAN-278)**: 두 모델 비교 보고서 `wiki/트랙비교_Gemini_vs_OpenAI.md`.
  구성별 점수 전체 + 세 가지 정반대(입력 글·차원·구조 효과) + 강약점. 공정 비교 어느 기준이든
  **Gemini ~0.06 앞섬**(0.9287 vs 0.8636~0.8701). 추가 유료 실험 불필요(공백 없음). 남은
  KAN-279 = 3~4축 종합 최종 선정.
- **T11 완료 · Phase 05 종료(2026-07-22, KAN-274)**: Gemini 최적 구성 선정 + 종합 보고서
  `wiki/트랙A_Gemini_실험종합.md`. **최고 SEMANTIC·768·짧은요약 @0.86 ARI 0.9287·F1 0.9302.**
  용도 5종 비교(비슷함재기 1위), 1536·상세요약·구조 다 못 넘음, 기준값 sharp peak, 비용 0.
  모델 노트 `gemini-embedding-001` 갱신. **트랙 A·B 모두 완료 → Phase 05 done, Phase 06
  ▶ current.** 공정 비교 Gemini 0.9287 vs OpenAI 0.8701(선정은 Phase 06).
- **T10 완료(2026-07-22, KAN-273)**: Gemini 구조 개선 실험 = 거의 효과 없음. 카테고리·주체
  파티션을 최고 구성(0.9287)에 얹어도 0.9287, 파티션 안 기준 완화(@0.84)로 겨우 0.9303
  (+0.0016). **OpenAI(+0.062)와 정반대** — 파티션은 잘못 합침을 막는 장치인데 Gemini는
  이미 거의 안 함. 남은 실수는 창·회색지대(LLM)·요약 품질 몫. 신호 파일
  `data/signals/articles90.json`(라벨 안 보고 손 추출, 두 트랙 공용) + `build_signals.py`
  + `experiment_structure.py` + `cluster_within_partitions`(테스트 4개). Gemini 최종은
  기준선 유지. 노트 `2026-07-22_..._STRUCTURE_partition`.
- **T09 후속 — 오류 사례 원인 규명(2026-07-22, KAN-272)**: 최고 구성의 오류 7건을
  유사도·발행 시각으로 분해 — ① 같은 인물 다른 사건 1건(홀란 0.8708>기준) ② 사가
  분할 2건(에데르송·잉글랜드 훈련 — 유사도는 넘는데 24h 창에 잘림; 창을 끄면
  0.9287→0.9100 하락이라 창 유지) ③ 같은 이슈 다른 발언 3건(0.79~0.86 경계 —
  기준 낮추면 손해, 임베딩 밖 신호 필요) ④ 노이즈 1건(검수 필요 원문 상이).
  개념 노트 '잘못 합침과 사가 분할'에 누적. T10 개선 목표: ③④, ②는 구조 밖.
- **T09 후속 — task_type 3종 추가(2026-07-22, KAN-272)**: 우리 문제와 맞는 나머지
  타입을 768·짧은요약 축에서 스윕 — FACT_VERIFICATION 0.8887 @0.77 ·
  CLASSIFICATION 0.8810 @0.90 · RETRIEVAL_DOCUMENT 0.8524 @0.88. 전부 SEMANTIC
  (0.9287)보다 낮음 → **task_type 5종 비교에서 SEMANTIC_SIMILARITY 확정 우위**.
  비대칭 검색용 3종(RETRIEVAL_QUERY 등)은 용도가 달라 제외.
- **T09 완료(2026-07-22, KAN-272)**: Gemini 8세트(task_type 2×차원 2×입력 2) 기준값
  0.01 스윕. 기준선 재측정 — 0.86에서 ARI 0.9287(기존 0.85/0.9071은 과소평가).
  최고 **SEMANTIC·768·짧은요약 @0.86 0.9287**. SEMANTIC이 CLUSTERING보다 항상 높고
  (0.9287 vs 0.8416), **상세요약은 Gemini에선 오히려 해로움**(0.9287→0.8343, OpenAI와
  반대). 남은 오류: 잘못 합침 1(홀란 응원 vs 교체 — 같은 인물 다른 사건), 잘못 나뉨 6
  (알론소 첼시 합류 3분할, 에데르송 맨유 무산 3분할 등). 무료 등급 분당 100요청 한도로
  세트 간 70초 간격 필요(재시도 백오프로는 부족).
- **T08 완료(2026-07-15, KAN-185)**: OpenAI 최적 구성 정리 → text-embedding-3 모델 노트.
  추천 large·제목+상세요약(점수 최우선 1536 @0.65 0.8077, 안정 우선 768 @0.60 0.7864).
  큰 발견: 기사 7351의 요약이 "검수 필요" 안내문이라 비어 있던 걸 제대로 채우니 OpenAI
  +0.056(→0.8636). Gemini는 그 기사에 영향 없어(원래 잘 묶음) 공정 비교 시 0.9287 vs
  0.8636 차이 ~0.065. 트랙 B(OpenAI) 4/4 완료 — Phase 05는 트랙 A(팀원) 대기.
- **"최근 24시간만 비교" 끄기 분석(2026-07-15)**: 이 규칙을 빼도 최고 ARI 거의 불변
  (OpenAI 미세 하락). 이 규칙은 점수에 거의 영향 없음. OpenAI-Gemini 격차(~0.11)는
  비교 시간 범위가 아니라 순수 임베딩 품질(같은 이름이면 다른 사건도 합침) — 규칙을
  꺼도 유지. (--no-note)
- **임계값 안정성 분석(2026-07-15)**: 상세요약 최고들 임계 상단(0.65~0.75) 재확인 —
  더 높은 점수 없음. large/1536은 기준값이 0.65일 때 점수가 가장 높지만 조금만 바꿔도
  많이 떨어져, 기준값을 정확히 맞춰야 한다. large/768(상세요약)은 기준값을 0.60~0.66
  어디에 두어도 점수가 비슷하게 유지된다. T08 선정은 기준값이 좀 달라져도 점수가
  버티는지 함께 봐야 함. (--no-note)
- **KAN-186 완료(2026-07-15)**: 상세요약 축 실험. 선행 스냅샷 articles_detail90
  (`fetch_summary_detail.py`) + title_detail 4세트. 상세요약이 1536차원을 크게
  끌어올려 **large/1536 @0.65 ARI 0.8077**(OpenAI 최고). 768은 이득 없음. 비교표는
  text-embedding-3 모델 노트. 브랜치 `feat/KAN-186-summary-detail`(feat/KAN-184 위 스택).
- **T07 후속 분석(2026-07-15)**: 임계값을 0.01 간격으로 촘촘히 훑어 최고 지점 보정 —
  진짜 최고 **large/768 @0.57 ARI 0.7976**(0.7545→). "제목만" 입력 축은 효과 없음
  (큰 모델에서 크게 하락) → 상세요약 축 유망. 입력 구성 모드(`embed_texts`)·
  `--no-note`·타임스탬프 충돌 수정 추가. 노트 4개 최고 지점으로 다시 만듦. 아직 T08 아님.
- **T07 완료(2026-07-15, KAN-184)**: OpenAI 짧은요약 4세트(small/large×768/1536)
  임계값 0.30~0.65 스윕·채점. 최고 large/768 @0.55 ARI 0.7545(Gemini 0.9071 대비 낮음).
  `scripts/sweep_threshold.py` 추가, 노트 이름에 모델·차원 반영(덮어쓰기 버그 수정).
  실험 노트 4개 + DECISIONS 기록. 브랜치 `feat/KAN-184-openai-experiment`.
- **컨벤션 이식(2026-07-15)**: plick-ai의 개발 규칙(Jira KAN·브랜치·커밋)과 구조
  (pipeline.md·develop 모델)를 전면 이식. develop 브랜치 생성, CLAUDE·AGENT_GUIDE·
  CONVENTIONS 재작성, pipeline.md 신설(현 상태 이관), PROGRESS는 얇은 포인터로 축소.
  이후 OpenAI 작업은 KAN-3 하위 티켓 단위로 진행. 브랜치 `chore/adopt-plickai-conventions`.
- **Phase 05 트랙 B 진행(2026-07-15)**: T05(매트릭스 설계)·T06(OpenAI 모듈) 완료.
  T07(짧은요약 4세트 실험)은 일부 실행(3/4세트) 후 이식 작업으로 중단 — 티켓 발행
  후 재개. 상세요약 축은 보류(스냅샷 선행 필요).
- **Phase 04 완료(2026-07-15)**: LLM Wiki 구축, 검토 통과.
- **Phase 03 완료(2026-07-15)**: 정답 라벨·채점기, 검토 통과. 라벨 원칙 "같은 이슈=
  똑같은 사건, 본문으로 판정"(7358 정정). 기준선 ARI 0.9071.
- **Phase 02 완료(2026-07-14)**: articles90 고정 + Gemini 기준선 실험·눈 검증.
- **Phase 01 완료(2026-07-14)**: 프로젝트 뼈대.
