# DECISIONS

> 스펙에 없던 설계 결정의 append-only 로그. 최신이 아래.
> 형식: `- YYYY-MM-DD [PNN] 결정 — 이유` (한 줄, 필요하면 두 줄까지)

- 2026-07-14 [init] 서버 프레임워크 없이 CLI 실험 리포로 — 실험·배치 작업이라
  HTTP 진입점 불필요. 서비스화 시 plick-ai(FastAPI) 합류가 팀 구조와 일치.
- 2026-07-14 [init] 벤치마크 대상은 Gemini·OpenAI API 임베딩 2종으로 한정 —
  사용자 결정. 자체 모델 학습·파인튜닝은 제외 범위.
- 2026-07-14 [init] 벤치마크는 2인 병렬 트랙(각자 모델 1개 담당)으로 진행,
  종합·선정은 별도 phase — 팀 협업 방식 반영. 트랙 간 비교를 위해 데이터셋·
  지표·노트 템플릿은 공통 고정.
- 2026-07-14 [init] 실험 기록 1차 저장소는 리포 내 Obsidian 보관함(`wiki/`) —
  실험마다 노트 자동 생성·git 공유. Confluence는 팀 공유용 요약으로 유지.
- 2026-07-14 [P02] 최근 24시간만 비교는 전역 군집화 후 "군집 내 이웃 간격 > 24h 지점
  분리"로 적용 (롤링 체인 유지) — PoC 원 구현을 확보하지 못해 SPEC 정의에서
  유도. T05 재현 실험에서 26/15 일치 여부로 검증하고, 다르면 재조정.
- 2026-07-14 [P02] 기존 실험 재현(26/15) 목표를 "새 데이터셋 기준선 확립"으로
  대체 — 원본 데이터셋(기사 56건 스냅샷·tmp_embeddings)이 소실됨. 새 스냅샷을
  data/articles.json으로 고정하고 잠정 구성으로 기준선 실험을 다시 기록한다.
- 2026-07-14 [P02] 새 데이터셋은 Supabase article_summaries의 PUBLISHED 기사
  중 published_at 2026-07-09~2026-07-13(UTC, 완결된 5일) 90건 — 50~100건 목표
  충족, 이적시장 기간이라 동일 이슈 중복 기대. 스냅샷은 git에 커밋해 고정하고
  fetch_articles.py는 기존 파일 덮어쓰기를 거부한다.
- 2026-07-14 [P03] Phase 03 계획을 새 90건 데이터셋 기준으로 개정 — 소실된
  56건 전제(published56.json, 잘못 합침 예시 제임스+라이스·기마랑이스+토날리)를
  제거하고, 라벨 파일을 data/labels/articles90.json으로, 정답 초안 시드를
  Phase 02 기준선 결과(results/20260714_204902 군집·눈 검증)로, 잘못 합침 완료
  조건 예시를 새 데이터 실제 사례(홀란 월드컵 미세뉴스·이라올라 계획+소감)로
  교체했다.
- 2026-07-14 [P03] 정답지 형식 = 평탄한 JSON {기사 id: 이슈 id}
  (data/labels/articles90.json). 같은 이슈 id = 정답 군집 하나. 로더는 키·값을
  문자열로 정규화하고, 라벨 없는 기사는 채점에서 제외하되 경고한다.
- 2026-07-14 [P03] 정답 라벨은 "실제 세계 사건 = 이슈 1개" 기준으로 24h
  비교 시간 범위와 무관하게 부여한다. 따라서 다일 걸친 사가(에데르송·틸레망스 등)를
  비교 시간 범위 제한이 쪼개면 잘못 나뉨으로 잡힌다 — 시간 범위 제한의 비용을 지표로 드러내려는 의도.
  임베딩 품질만 보고 싶으면 시간 범위 제한 없이 실험과 비교한다(향후).
- 2026-07-14 [P03] SEMANTIC@0.85 기준선 점수(90건, 768d, 24h): ARI 0.9071,
  쌍 단위 정밀도 0.9412·재현율 0.8791·F1 0.9091 (TP 80/FP 5/FN 11),
  잘못 합침 2건(홀란 경험/교체/응원, 이라올라 계약+계획)·잘못 나뉨 6건.
  results/20260715_102026/. 이후 모든 실험의 비교 기준선.
  (개발자 검수 반영: 7358 "맨유, 두 미드필더 £85m 발표 임박"은 틸레망스
  명시가 없어 tielemans_manutd→manutd_two_midfielders_85m 단독 이슈로 정정.
  정답지의 시각(타이밍) 기반 오추론을 본문 기준으로 교체 — 라벨 원칙은
  "같은 이슈 = 똑같은 사건, 본문으로 판정".)
- 2026-07-15 [P03→P05 이월] 열린 항목 2개(라벨 세밀도·입력 텍스트 구성)는
  지금 확정하지 않고 Phase 05에서 함께 정리한다. (1) 이적 사가를 한 사건으로
  볼지 소식 단위로 쪼갤지 — 개발자는 "시간 순 다른 사건"으로 소식 단위 분리에
  기울지만 확정 아님. 소식 단위로 가면 24시간 비교 규칙이 오히려 정답과 맞아
  "잘못 나뉨"이 줄어듦. 현재 기준선은 "사가=1개" 유지. (2) 임베딩에 넣는 텍스트
  구성을 Phase 05 비교 축으로 추가 — 지금은 제목+짧은 요약(summary_short).
  확인 결과 article_summaries에 summary_detail(~148자, 짧은 요약의 3.5배)이
  있어 "제목만 / 제목+짧은요약 / 제목+상세요약" 비교 가능. category·rumor_stage
  메타데이터도 존재(향후 활용 여지). 입력 텍스트를 바꾸면 라벨도 함께 재검토.
- 2026-07-15 [P04] Phase 04 설계를 카파시 LLM Wiki 패턴(gist 442a6bf) 및
  실전 가이드 2건과 대조 검토 — 골격(마크다운+[[링크]]+목차, 원본/위키/규칙
  3계층, 코드에 의한 결정적 노트 자동 생성)은 정합 판정. 보강 3가지를 phase에
  반영: (1) INDEX에 위키 작성 규칙 절(이름·머리말·목차 갱신 기준), (2) 실험
  노트는 results/ 없이 자급자족(점수·대표 사례 본문 포함 — results/는
  gitignore라 팀원에게 안 보임), (3) 깨진 [[링크]]·목차 누락 자동 점검
  테스트를 완료 조건에 추가. log.md·raw 폴더·임베딩 검색은 우리 규모(노트
  수십 개, 2인)에 불필요 판단으로 미채택.
- 2026-07-15 [P04] 실험 노트 이름 규칙 = YYYY-MM-DD_<데이터셋><건수>_<task
  앞토큰>_<임계>_<비교범위>h (예: 2026-07-15_articles90_SEMANTIC_0.85_24h).
  report/wiki.py가 실행 시 자동 생성하고 00-INDEX.md 실험 기록 표에 행을
  추가/교체(멱등)한다. 과거 실험은 Confluence 실험 기록 폴더(14843913)에서
  백필. 위키 노트는 쉬운 말 규칙을 따르며 결정적(같은 실행=같은 노트).
- 2026-07-15 [P05] Phase 05를 "공동 합의" 대신 트랙별 독립 설계로 개정 —
  T01은 공통 기준(데이터셋·지표·양식·공유 축)만 고정하고, 실험 매트릭스는
  각 트랙이 자기 wiki 계획 노트로 따로 설계한다. 담당: 트랙 A(Gemini)=팀원,
  트랙 B(OpenAI)=개발자 본인. 작업 재편 T01(공통)+A 3개(T02~T04)+B 4개
  (T05~T08). 트랙 간 비교는 공통 기준으로 성립.
- 2026-07-15 [P05] 입력 텍스트 구성을 정식 공유 실험 축으로 확정 —
  "제목+짧은요약"과 "제목+상세요약(summary_detail)" 둘 다 두 트랙 공통으로
  비교(개발자 결정). 선행 작업: data/articles.json에 summary_detail 필드가
  없어(P02-T01 스냅샷 미포함), 같은 90건에 필드만 추가한 스냅샷이 필요
  (정답 라벨 그대로 유효, 트랙 A·B 공용). 이 전엔 "제목+짧은요약" 축만 실행 가능.
- 2026-07-15 [meta] plick-ai 개발 컨벤션·구조 전면 이식 — agentic-starter 골든
  루프(PROGRESS 단일 소스·phase-runner·main 직접 커밋)를 폐기하고 plick-ai 방식
  채택: main←develop←`<type>/KAN-###-<slug>` 브랜치, 커밋 `<type>: 요약 (KAN-###)`
  (한 태스크=한 커밋, Co-Authored-By 금지, 커밋 자동·푸시 승인), 진행 관리는
  docs/pipeline.md(현황표+태스크+로그). CLAUDE·AGENT_GUIDE·CONVENTIONS 재작성,
  pipeline.md 신설(현 상태 이관), PROGRESS는 포인터로 축소. Jira는 프로젝트 KAN,
  임베딩 작업은 에픽 KAN-3 "[기능] AI 가공" 하위 작업 티켓. 사유: 팀 공용 규칙을
  이 리포에도 일치시켜 티켓 단위로 관리·이식.
- 2026-07-15 [P05] 실험 노트 이름 규칙에 모델·차원 추가 —
  `..._<모델태그>-d<차원>_<task>_<임계>_...` (예: articles90_oai-large-d768_none_0.55).
  OpenAI는 task_type이 모두 `none`이라 기존 규칙(task·임계만)으로는 4세트가
  이름이 겹쳐 노트가 덮어써지는 잠재 버그가 있었다(Gemini도 768/1536 동일 임계면
  충돌). 모델 태그·차원을 넣어 (모델×차원×임계)마다 고유. 모델 링크는 OpenAI
  small/large가 wiki/models/text-embedding-3.md 한 노트를 공유하도록 매핑.
- 2026-07-15 [P05] T07 임계값 스윕 = 세트마다 임베딩 1회(캐시) 후 임계값
  0.30~0.65를 0.05 간격으로 훑어 최고 ARI 지점만 노트로 남긴다
  (`scripts/sweep_threshold.py`). 임계값 훑기는 벡터 재사용이라 API를 다시 안 부른다.
  트랙 B 짧은요약 4세트 최고: **large/768 @0.55 ARI 0.7545·F1 0.7604**
  (small/768 0.7262, small/1536 0.7236, large/1536 0.7371). Gemini 기준선 0.9071 대비
  낮음 — Phase 06 비교 재료. (아래 촘촘한 스윕에서 최고 지점 보정됨.)
- 2026-07-15 [P05] 임계값 스윕을 0.05→0.01 간격으로 촘촘히 하니 진짜 최고 지점이
  이동 — large/768은 0.55(0.7545)가 아니라 **0.57에서 ARI 0.7976·F1 0.8022**(트랙 B
  짧은요약 진짜 최고), large/1536은 0.58에서 0.7568, small은 0.53에서 0.7262. 성긴
  간격(0.05)이 OpenAI를 과소평가했다. 4개 실험 노트를 촘촘한 최고 지점으로 다시 만듦
  (옛 0.55/0.5 노트 대체). 이후 스윕은 필요 구간을 0.01 간격으로 훑는다.
  Gemini(0.9071)와 격차 0.15→0.11로 축소.
- 2026-07-15 [P05] 입력 구성 "제목만(title)" 축 실험 = 효과 없음 — 짧은요약을 빼면
  나빠지고 특히 큰 모델에서 크게 하락: large/768 0.7976→0.7082, large/1536
  0.7568→0.7126, small은 거의 불변(0.7262→0.7148/0.7236). 즉 큰 모델일수록 본문
  텍스트를 많이 활용한다(같은 이름이면 합치는 걸 본문이 눌러줌) → 입력을 더 풍부하게
  (summary_detail) 주는 축이 유망하다는 방증. 제목만은 최적 임계도 0.62~0.64로 상승
  (짧은 텍스트=유사도 전반 상승). 노트는 남기지 않고(효과 없음) 이 로그로 갈음.
  입력 구성 모드는 `pipeline/articles.py::embed_texts`(title/title_short), 노트
  이름·config에 input_text 반영(기본 title_short는 이름에 미표기).
- 2026-07-15 [P05/KAN-186] 상세요약(제목+상세요약) 축 실험 = 고차원에서 큰 개선.
  선행: 같은 90건에 summary_detail만 채운 data/articles_detail.json 생성
  (`scripts/fetch_summary_detail.py`, id 기준 보강, 평균 81자). 결과(0.01 스윕 최고
  ARI): small/768 0.7114, small/1536 0.7751, large/768 0.7864, **large/1536 0.8077**
  (@0.65, F1 0.8118 — OpenAI 통틀어 최고). 상세요약은 1536차원을 크게 끌어올리나
  (large/1536 +0.051, small/1536 +0.049) 768차원은 이득 없음(large/768 0.7976→0.7864).
  Gemini 0.9071과 격차 0.10. 최종 선정은 T08/Phase 06. 비교는 [[text-embedding-3]]
  모델 노트에 기록.
- 2026-07-15 [P05] 상세요약 최고들의 임계값 상단(0.65~0.75)을 더 훑어봐도 더 높은
  점수는 없었다(기록값 유효). 다만 기준값이 조금 달라졌을 때 점수가 얼마나 버티는지가
  구성마다 크게 다르다 — large/1536(상세요약)은 기준값이 0.65일 때 점수가 가장
  높지만(0.8077), 0.64나 0.66으로 조금만 바꿔도 0.77 아래로 떨어진다. 즉 기준값을
  정확히 맞춰야만 좋은 점수가 나온다. 반대로 large/768(상세요약)은 기준값을 0.60에서
  0.66 사이 어디에 두어도 점수가 0.78 정도로 비슷하게 유지된다. 그래서 "점수가 가장
  높은 구성"과 "기준값이 좀 달라져도 안정적인 구성"이 다르다 → T08 선정은 점수만
  보지 말고 기준값이 조금 어긋나도 점수가 유지되는지 함께 본다. (짧은요약 large/768
  @0.57 0.7976도 그 기준값에서만 점수가 높다.) 분석은 --no-note 스윕, 노트 불변.
- 2026-07-15 [P05] "최근 24시간만 비교"를 끄는 분석 = 이 규칙은 점수에 거의 영향 없음.
  비교 시간 범위를 사실상 무한대로 줘서 사가 분할을 없애고 다시 훑은 결과, 최고 ARI가
  거의 안 변하고 OpenAI는 오히려 미세 하락: gemini 0.9071→0.9100, large/768 짧은요약
  0.7976→0.7865, large/768 상세요약 0.7864→0.7842, large/1536 상세요약 0.8077→0.7980.
  최적 임계값도 그대로(0.85→0.86 등). 사가 분할로 잃는 만큼, 시간이 떨어진 다른 이슈를
  갈라주던 이득도 사라져 상쇄됨. **핵심: OpenAI가 Gemini보다 낮은 건 "최근 24시간만
  비교" 때문이 아니라 순수 임베딩 품질**(같은 이름이면 다른 사건도 합침) — 이 규칙을
  꺼도 격차 ~0.11 유지(0.798 vs 0.910). 분석은 --no-note.
- 2026-07-15 [P05] 최고 구성 사례 분석(잘못 합침/나뉨 + 잘 묶인 것). 잘 묶임: 산투스
  맨유 6건·기마랑이스 5건·메리노 3건 등 "같은 선수+구단+반복 협상" 사가는 안정.
  실패 유형 3종 — (1) 같은 이름이면 다른 사건도 합침(홀란 4이슈 뭉침, 상세요약이 4→2로
  완화), (2) 언어 차이(틸레망스 사가에서 영어 트윗만 따로 떨어짐, 두 구성 공통),
  (3) 여러 날 이어진 사가가 쪼개짐(alonso·ederson, 라벨 "사건=1개"와 임베딩 거리의
  충돌). 상세요약↔짧은요약은 "합쳐야 할 걸 덜 뭉침 ↔ 같은 걸 더 쪼갬"을 맞바꾼다.
  wiki [[잘못 합침과 사가 분할]] 개념 노트에 반영.
- 2026-07-15 [P05] 데이터 품질이 임베딩 구성보다 점수를 더 크게 올린다 — 기사 한 건
  (7351)이 요약 단계에서 걸러져 진짜 요약 대신 "검수 필요" 안내문만 있었다. 이 요약을
  제대로 된 한글로 바꾸자 OpenAI 점수가 0.056 올랐다(large/1536 상세요약 0.8077→0.8636,
  large/768 짧은요약 0.7976→0.8552). tielemans 사가가 온전히 합쳐짐. 이런 안내문 기사는
  90건 중 1건뿐이라 이상치지만, 큰 사가 구성원 1명이 어긋나면 ARI가 크게 흔들린다.
  방법: 요약은 손으로 옮긴 what-if(고정 데이터는 안 바꿈). 시사점: 상류 요약 품질이
  가장 큰 지렛대. (이 what-if 요약 검증 스크립트는 scratchpad, 리포 미포함.)
- 2026-07-15 [P05/KAN-185] 7351을 고쳐 Gemini와 공정 비교 = 이 문제는 OpenAI만 힘들게
  했다. Gemini는 7351이 부실해도 원래부터 tielemans 사가에 제대로 묶었고(쪼개지 않음),
  요약을 고쳐도 점수 변화 없음(0.9287 그대로). 깨끗한 데이터에서 각자 최고: Gemini
  0.9287(기준값 0.86) vs OpenAI large/1536 상세요약 0.8636 → 차이 ~0.065(원래 보이던
  0.10보다 좁음). 덤: Gemini 기준선도 기준값을 0.01로 촘촘히 훑으면 0.9071(@0.85)→
  0.9287(@0.86)로 오른다 — 트랙 A(팀원)도 재스윕 필요, 공유 대상.
- 2026-07-15 [P05→P07] 사례 분석 결론: **점수를 더 올리는 건 임베딩 튜닝이 아니라
  구조**다. 순수 임베딩은 "같은 선수+팀"을 지배적으로 봐서 사건 종류·단계가 묻힌다.
  대신 신호를 순서로 쓴다 — category(1차 구분) → 선수·팀 개체 → rumor_stage(단계) →
  임베딩(나머지 유사도) → LLM(애매한 것). 근거: 순수 임베딩 천장(OpenAI ~0.81, Gemini
  ~0.93)과 남은 실수의 성격. 최종 이식처 plick-ai에서 이 순서 구현 목표(Phase 07).
  상세·근거는 wiki [[묶기에 쓸 신호와 구조]].
- 2026-07-15 [P05] 앞으로 실험에서 쓸 수 있는 신호(Supabase article_summaries) 정리 —
  **category**(TRANSFER 52·OTHER 19·MATCH 13·FITNESS 6; 2건+ 정답 이슈 15개 중 14개
  카테고리 일관 → 1차 구분 안전, iraola만 걸침), **rumor_stage**(IN_PROGRESS/CONFIRMED
  = 추측/확정; 단 라벨 노이즈 있고 금액·메디컬·희망 세부 필드는 없음), **content_type**
  (현재 전부 GENERAL·무의미), 입력 텍스트(제목·summary_short·summary_detail). **원문
  기사 본문은 DB에 없음**(넣어도 잡음·개체 과합침 우려로 비권장). 상세는 위 wiki 노트.
- 2026-07-15 [P05→P07] 카테고리+주체(개체) 구분 실험으로 구조 가설 검증(최고 구성
  large/1536 상세요약). 임베딩만 0.8077 → +카테고리(다른 카테고리 안 묶음) 0.8126 →
  **+주체(주체 다르면 안 묶음) 0.8701**(+0.06). 모델·임베딩 그대로, 신호만으로 Gemini
  깨끗한 데이터(0.9287)와 격차 ~0.06까지 축소. 부실한 7351도 제목의 주체(틸레망스)로
  공짜 복구(잘못 나뉨 7→6). 주체는 라벨 안 보고 손 추출(운영은 LLM). **남은 잘못 합침
  1건(홀란 벨링햄 극찬 vs 잉글랜드 응원)은 LLM 자리** — 주체 같고 rumor_stage 없고,
  임베딩도 진짜 쌍 0.703 vs 거짓 쌍 0.670(차 0.033)이라 어떤 기준값으로도 못 가름.
  결론: 카테고리+주체로 상당 부분, 나머지 애매한 건 LLM. 상세는 wiki [[묶기에 쓸 신호와 구조]].
- 2026-07-22 [P05/KAN-274] Gemini 최적 구성 선정 + 종합 보고서. **최고 = SEMANTIC_SIMILARITY·
  768·제목+짧은요약·기준값 0.86, ARI 0.9287·F1 0.9302.** 용도 5종 중 비슷함재기 1위
  (사실검증 0.8887·분류 0.8810·검색문서 0.8524·묶기용 0.8416), 1536은 0.9178로 낮고
  상세요약은 해로움(0.834~0.843, OpenAI와 반대), 구조 파티션 +0.0016. 기준값은 sharp
  peak(0.85~0.86 최고, 0.87서 0.854로 급락) → 경계는 LLM. 비용 무료 등급 0원(분당 100 한도).
  보고서 [[트랙A_Gemini_실험종합]], 모델 노트 [[gemini-embedding-001]] 갱신. **트랙 A 3/3
  완료 → Phase 05(두 트랙) 완료.** 공정 비교 Gemini 0.9287 vs OpenAI 0.8701(Phase 06에서 선정).
- 2026-07-22 [P05/KAN-273] Gemini 구조 개선 실험 = 거의 효과 없음(OpenAI와 정반대).
  카테고리·주체 파티션을 최고 구성(SEMANTIC·768·짧은요약 @0.86, 0.9287)에 얹어도
  0.9287 그대로, 파티션 안 기준값을 0.84로 낮춰야 겨우 0.9303(+0.0016). OpenAI는
  같은 구조로 +0.062였다. 이유: 파티션은 "다른 주체·분야 잘못 합침"을 막는 장치인데
  Gemini는 그걸 이미 거의 안 함(강한 임베딩). +카테고리가 홀란 응원(OTHER)/교체(MATCH)는
  갈랐으나 홀란 골저지/교체(둘 다 MATCH·홀란)가 남아 상쇄. **결론: 구조는 약한 임베딩을
  구제하는 장치. Gemini 최종 구성은 기준선 유지, 남은 실수는 창·회색지대(LLM)·요약 품질
  몫.** 신호 파일 `data/signals/articles90.json`(build_signals.py, 라벨 안 보고 손 추출,
  두 트랙 공용), 실험 `scripts/experiment_structure.py`, 파티션 군집화는
  `pipeline/clustering.py::cluster_within_partitions`(테스트 포함). 노트
  [[2026-07-22_articles90_gemini_STRUCTURE_partition]].
- 2026-07-22 [P05→P07/plick-ai v2] 운영 묶기 설계 확정(KAN-275) — 주체(**타입〔선수·
  감독·구단주 등, 우리 카탈로그 타입〕 + 영어 이름**)·카테고리는 1단계 분류 LLM이
  요약과 함께 출력(주체 찾기는 벡터 비교가 아니라 추출). 이름을 figures 카탈로그에
  대조해 매칭되면 표준 인물/팀, **미매칭이면 "미매칭 키워드"로 저장**(admin-server
  소유, plick-ai는 stateless로 주체만 반환). 타입은 같은 이름 다른 역할을 갈라 준다.
  실시간은 증분 묶기: 기사당 임베딩 1회 저장 → (카테고리·주체·최근 N시간) 필터
  top-k 조회 → 회색지대만 LLM. **전체 재임베딩 아님**(24h는 저장 벡터의 조회 조건).
  ④ 노이즈("검수 필요" placeholder)는 묶기가 아니라 요약 단계 품질 게이트에서 처리.
  plick-ai README §8.1 v2 설계 절에 반영. 데이터 입력은 category + 주체(타입+영어 이름).
- 2026-07-22 [P05/KAN-272] 트랙 A(Gemini) 인수 — 8세트 임계 0.01 스윕 + task_type
  5종 비교. 최고 SEMANTIC_SIMILARITY·768·짧은요약 @0.86 ARI 0.9287(기준선 0.85/0.9071
  은 과소평가). SEMANTIC이 전 구성 우위, 상세요약은 Gemini엔 해로움(OpenAI와 반대).
  오류 7건 4갈래(같은 인물 다른 사건·사가 분할·발언 차이·노이즈) → 위 v2 구조로 대응.
- 2026-07-15 [P05] 임베딩 쪽 마지막 두 카드 = 효과 없음(음성). 최고 구성(large/1536
  상세요약, 기준 0.8077)에 (1) 군집화 방식을 average→complete로 바꾸기, (2) 임베딩에서
  모두가 공유하는 공통 방향(평균·상위 주성분) 빼기를 시도. complete는 0.75로 더 나쁘고,
  공통 방향 빼기는 0.80으로 살짝 낮아짐(합침↔나뉨 균형만 바뀌고 총점 안 오름, 많이
  빼면 급락). 즉 지금 average·후처리 없음이 이미 최선. **결론: 임베딩 하이퍼파라미터+
  기법까지 다 소진, 더 높은 성능의 길은 임베딩이 아니라 구조(카테고리+주체+LLM).**
